%===============================================================================================
% VARIANT D — C's section order, LEFT-ALIGNED masthead with a rule; pipe-separated skills.
%===============================================================================================
% The only variant whose masthead is not centred: the name, headline slot and contact line all sit
% flush left, closed by a rule across the text width. That is the difference visible from across
% the room, and it fires for every profile — which is the point. D's old sole differentiator was
% the project tech stack on its own line, which vanished for any profile that recorded no tech
% stack, leaving D byte-identical to C.
%
% It keeps that tech-stack treatment as a bonus: where C, E and every other variant put a project's
% technologies inline after a pipe, D gives them their own bold "Technologies:" line beneath the
% title. That is a CONTENT-builder difference (buildProjectItems reads the variant's
% projectTechLayout), not a template one, so there is nothing here to keep in sync with C.
%
% Skills within a category are joined with " | "; coursework runs in three columns.
%===============================================================================================

%%PREAMBLE%%
%%STYLE d%%

\begin{document}

\resumeHeader{NAME}{}{CONTACT_LINE}

%%SECTION summary%%
%%SECTION skills%%
%%SECTION experience%%
%%SECTION education%%
%%SECTION certifications%%
%%SECTION projects%%
%%SECTION coursework%%
%%SECTION extracurricular%%

\end{document}
